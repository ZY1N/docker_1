docker swarm init --advertise-addr $(docker-machine ip Char)

#--advertise-addr flag configures manager node to publish its address as 192.168.99.100
#docker swarm join-token manager to add another manager