eval $(docker-machine env Char)
#docker-machine env hooks up docker command to run against a machine
#eval is part of POSIX- It will take an argument and construct a command of it, which will be executed by the shell.
#note you don't really need the eval command, but it's suggested. It concatinates things